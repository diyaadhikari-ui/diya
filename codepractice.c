#include <stdio.h>

int main() {
    int age;
    char citizen;

    // Asking user for age and citizenship status
    printf("Enter your age: ");
    scanf("%d", &age);

    printf("Are you a citizen? (yes/no): ");
    scanf(" %c",&citizen): // space before %c to consume

